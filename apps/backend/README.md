# Backend

## Development
```sh
npm install
npm run dev
```

Open [localhost:5000](http://localhost:5000)

## Building
```sh
npm install
npm run build
cp dist <target> # simulate copy to server. <target> should be outside of the backend folder.
cd <target>
node .
```
